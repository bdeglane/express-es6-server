import Controller from '../../Controller';

export default class ExperimentController extends Controller {
  constructor() {
    super('experiment');
  }

  getById(req, res) {
  }

  post(req, res) {
  }

  update(req, res) {
  }

  remove(req, res) {
  }
}